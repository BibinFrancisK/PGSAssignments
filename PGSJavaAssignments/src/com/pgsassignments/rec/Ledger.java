package com.pgsassignments.rec;

import java.util.Date;

public class Ledger {
/*	
	private Account[] arrayOfAccounts;
	private int ledgerNumber;
	private Date ledgerStartDate;
	
	public String printStatement(int accountNumber) {
		
		for (int i = 0; i < arrayOfAccounts.length; i++) {
			if(arrayOfAccounts[i].accountNumber == accountNumber)
				return "AccountNumber= " + arrayOfAccounts[i].accountNumber
						+"\nLedgerNumber= " + ledgerNumber
						+"\nBalance= " + arrayOfAccounts[i].balance;
					}	
		return "Account not found!"
	}
*/
}
